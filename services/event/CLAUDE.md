# CLAUDE.md — event-service

> Loaded when working inside `services/event/`. See root `CLAUDE.md` for shared conventions.

Placeholder — populated in Phase 5 (venues/events/seat_categories/seats catalog endpoints). Owns:
static seat map + pricing, read-heavy catalog. Never tracks seat sale state — see booking
(ADR-0001).
