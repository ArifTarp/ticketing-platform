<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.demo.ticketing</groupId>
    <artifactId>ticketing-platform</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>notification</artifactId>
  <packaging>jar</packaging>
  <name>notification</name>

  <properties>
    <!-- Same patch-level bump as services/payment/booking/event: newer than the Spring Boot
         3.3.x BOM's managed version (1.19.8), kept in sync across modules so every Testcontainers
         suite in the repo runs on one version. -->
    <testcontainers.version>1.20.4</testcontainers.version>
  </properties>

  <!-- Overrides the Spring Boot BOM's testcontainers-bom import (1.19.8) — a child's own
       dependencyManagement entry takes precedence over an inherited one, unlike a plain property
       override, which the parent's already-imported/resolved BOM entries would ignore. -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- Shared Kafka command/event DTOs + producer/consumer/DLQ config support, per
         docs/adr/0003-shared-messaging-module-for-kafka-contracts.md. -->
    <dependency>
      <groupId>com.demo.ticketing</groupId>
      <artifactId>messaging</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- notification is a pure Kafka consumer with no inbound REST (per its own CLAUDE.md) —
         starter-web is here only because actuator needs a servlet container to expose
         health checks over HTTP; do not add a business REST controller to this module. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.kafka</groupId>
      <artifactId>spring-kafka</artifactId>
    </dependency>
    <!-- Structured JSON console logs (root CLAUDE.md: "Structured JSON logs everywhere"), consumed
         by the Elastic/Kibana log stack. 7.4 is the last line explicitly compatible across
         Logback 1.2/1.3/1.4/1.5.x without a hard floor above our managed 1.5.12 (Spring Boot
         3.3.6 BOM), so it avoids a NoSuchMethodError risk that newer 8.x releases (which require
         logback-classic >= 1.5.13) could hit. -->
    <dependency>
      <groupId>net.logstash.logback</groupId>
      <artifactId>logstash-logback-encoder</artifactId>
      <version>7.4</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- EmbeddedKafka for the Kafka-wiring test, mirroring services/payment's
         PaymentKafkaWiringTest and the messaging module's KafkaRoundTripTest. -->
    <dependency>
      <groupId>org.springframework.kafka</groupId>
      <artifactId>spring-kafka-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.awaitility</groupId>
      <artifactId>awaitility</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
