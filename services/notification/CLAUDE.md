# CLAUDE.md — notification-service

> Loaded when working inside `services/notification/`. See root `CLAUDE.md` for shared
> conventions.

Placeholder — populated in Phase 9 (consumes `booking.events`, idempotent on event id, logs
"sent" mail/SMS mock). Owns: `notifications` table. Pure consumer, no inbound REST.
