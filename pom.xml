<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.demo.ticketing</groupId>
  <artifactId>ticketing-platform</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>ticketing-platform</name>
  <description>Event ticketing platform monorepo (parent aggregator)</description>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <!-- spring-boot-starter-parent normally turns this on; we use our own parent, so set it
         explicitly. Without it, Spring cannot infer @PathVariable/@RequestParam names from method
         parameters and fails at runtime with "parameter name information not available". -->
    <maven.compiler.parameters>true</maven.compiler.parameters>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <!-- Keep these two in step: spring-cloud 2023.0.4 ships spring-cloud-gateway-server 4.1.6,
         whose ForwardedHeadersFilter calls HttpHeaders.headerSet() — an API that only exists from
         Spring Framework 6.1.15, i.e. Spring Boot >= 3.3.6. On Boot 3.3.4 every proxied request
         died with NoSuchMethodError at runtime (found in Phase 4 by the gateway route tests). -->
    <spring-boot.version>3.3.6</spring-boot.version>
    <spring-cloud.version>2023.0.4</spring-cloud.version>
    <resilience4j.version>2.2.0</resilience4j.version>
  </properties>

  <modules>
    <module>gateway</module>
    <module>services/auth</module>
    <module>services/event</module>
    <module>services/booking</module>
    <module>services/payment</module>
    <module>services/notification</module>
  </modules>

  <!-- Applies to every module: Spring Boot's default TestRestTemplate factory
       (SimpleClientHttpRequestFactory) writes POST/PUT bodies in streaming mode and cannot
       retry/read a 4xx response afterwards (HttpRetryException: "cannot retry due to server
       authentication, in streaming mode"). Adding this on the test classpath makes Spring Boot
       auto-select HttpComponentsClientHttpRequestFactory instead — no test code changes needed.
       Found in Phase 4/5 verification: every integration test asserting a 401/403/409 response
       body via TestRestTemplate needs it. -->
  <dependencies>
    <dependency>
      <groupId>org.apache.httpcomponents.client5</groupId>
      <artifactId>httpclient5</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.github.resilience4j</groupId>
        <artifactId>resilience4j-spring-boot3</artifactId>
        <version>${resilience4j.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
