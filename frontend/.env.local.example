# Base URL of the gateway (the only backend the frontend talks to). Defaults to
# http://localhost:8080 in code (lib/constants.ts) if this isn't set — only needed when the
# gateway is running somewhere else (e.g. a different port during local development).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
